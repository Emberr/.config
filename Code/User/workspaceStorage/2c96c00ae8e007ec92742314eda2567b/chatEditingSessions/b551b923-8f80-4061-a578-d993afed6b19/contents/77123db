## Autostart commands
#
# This file contains commands that Hyprland will run once when it starts.
# Use `exec-once` for things that should only start once per session and
# `exec` for commands that should run every time the config is reloaded.

autostart {
    # Import the system's DBus environment variables so that apps launch
    # correctly under a Wayland session
    exec-once = dbus-update-activation-environment --systemd DISPLAY WAYLAND_DISPLAY XDG_CURRENT_DESKTOP

    # Start the wallpaper daemon.  Hyprpaper reads `~/.config/hyprpaper.conf`.
    exec-once = hyprpaper

    # Launch Waybar as the status bar
    exec-once = waybar

    # Start a clipboard manager (optional).  Uncomment if installed.
    # exec-once = wl-clipboard-history -l 25 -t 5000

    # Start a lock/idle daemon (optional).  You can use hypridle or
    # xidlehook; here’s an example with hypridle that locks after 5 minutes
    # of inactivity and suspends after 15 minutes.
    # exec-once = hypridle -c ~/.config/hypridle.conf

    # Restore the last wallpaper and colour scheme
    exec-once = ~/.config/hypr/scripts/update-colors.sh
}