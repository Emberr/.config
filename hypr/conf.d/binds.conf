## Keybindings
##
# This file defines keyboard shortcuts for launching applications,
# managing windows and workspaces, and taking screenshots.  The
# `$mod` variable holds the main modifier key (usually the Super or
# Windows key).  Combine modifiers with `|` (e.g. `$mod|SHIFT`).

## Define custom variables for readability
$mod = SUPER
$shift = SHIFT
$ctrl = CTRL

## Launch terminal (Kitty) in floating mode
#
# We pass a unique class (`kitty-floating`) to kitty so that
# Hyprland's window rules can match it and force the window to be
# floating and centered.  See windowrules.conf for the matching rules.
bind = $mod, Return, exec, kitty
bind = $mod|$shift, Return, exec, kitty --class kitty-floating

## Application launchers and tools
# Use our custom Wofi menu script that provides submenus for apps,
# wallpaper switching, terminal, power menu, etc.  You can still
# launch a plain Wofi application search by selecting the “Apps” entry
# in that menu.
bind = $mod, R, exec, ~/.config/hypr/scripts/wofi-menu.sh

# File manager (Nautilus)
bind = $mod, E, exec, nautilus

# Code editor (VS Code)
bind = $mod, S, exec, code

## Web browser
# Launch Zen Browser directly into private/incognito mode.  If Zen
# supports a different flag for private browsing, adjust the argument
# accordingly.
bind = $mod, W, exec, zen-browser
bind = $mod|$shift, W, exec, zen-browser --private

## Process control
# Kill the active window
bind = $mod, C, killactive

## Emoji picker
bind = $mod, M, exec, ~/.config/hypr/scripts/wofi-emoji.sh

## Floating toggle
bind = $mod, F, exec, hyprctl dispatch togglefloating

## Move focus between windows (vim‑like)
bind = $mod, H, movefocus, l
bind = $mod, J, movefocus, d
bind = $mod, K, movefocus, u
bind = $mod, L, movefocus, r

## Move windows around
bind = $mod|$shift, H, movewindow, l
bind = $mod|$shift, J, movewindow, d
bind = $mod|$shift, K, movewindow, u
bind = $mod|$shift, L, movewindow, r

## Resize windows
bind = $mod|$ctrl, H, resizeactive, -50 0
bind = $mod|$ctrl, J, resizeactive, 0 50
bind = $mod|$ctrl, K, resizeactive, 0 -50
bind = $mod|$ctrl, L, resizeactive, 50 0

## Workspace switching (1–9)
bind = $mod, 1, workspace, 1
bind = $mod, 2, workspace, 2
bind = $mod, 3, workspace, 3
bind = $mod, 4, workspace, 4
bind = $mod, 5, workspace, 5
bind = $mod, 6, workspace, 6
bind = $mod, 7, workspace, 7
bind = $mod, 8, workspace, 8
bind = $mod, 9, workspace, 9

## Move active window to workspace
bind = $mod|$shift, 1, movetoworkspace, 1
bind = $mod|$shift, 2, movetoworkspace, 2
bind = $mod|$shift, 3, movetoworkspace, 3
bind = $mod|$shift, 4, movetoworkspace, 4
bind = $mod|$shift, 5, movetoworkspace, 5
bind = $mod|$shift, 6, movetoworkspace, 6
bind = $mod|$shift, 7, movetoworkspace, 7
bind = $mod|$shift, 8, movetoworkspace, 8
bind = $mod|$shift, 9, movetoworkspace, 9

## Mouse bindings
# Hold super and left click to move windows and right click to resize
bindm = $mod, mouse:272, movewindow
bindm = $mod, mouse:273, resizewindow

## Jump to last active workspace
bind = $mod, Tab, workspace, previous

## Screenshot bindings using Hyprshot
bind = $mod, PRINT, exec, hyprshot -m window
bind = , PRINT, exec, hyprshot -m output
bind = $mod|$shift, PRINT, exec, hyprshot -m region

## Launch system monitors in a floating terminal
# Use a unique class so that window rules can float these windows too
bind = $mod, T, exec, kitty --class kitty-floating -e btop
bind = $mod|$shift, T, exec, kitty --class kitty-floating -e gotop

## Launch Yazi file manager in a floating terminal
bind = $mod, N, exec, kitty --class yazi-floating -e yazi

## Reload Hyprland and update colours
bind = $mod|$shift, R, exec, ~/.config/hypr/scripts/update-colors.sh

## Lock the screen with Hyprlock
bind = $mod|$shift, L, exec, hyprlock