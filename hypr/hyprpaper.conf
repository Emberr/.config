### Preload your wallpaper so it's ready at start
preload = $HOME/Pictures/wallpapers/wats.jpg

# Apply it to all monitors (or specify a monitor name)
wallpaper = , $HOME/Pictures/wallpapers/wats.jpg

# Preload your wallpaper so it’s ready at start
# preload = $HOME/Pictures/wallpapers/wats.jpg

# Apply it to all monitors (or specify a monitor name)
# wallpaper = , $HOME/Pictures/wallpapers/wats.jpg

# Enable IPC so hyprctl commands work
ipc = true
